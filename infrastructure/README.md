# Infrastructure

Reserved for infrastructure-as-code and deployment configuration (e.g. container orchestration manifests, environment provisioning). Empty as of Phase 1 — local development infrastructure (Postgres, Redis, API container) is defined in the root [docker-compose.yml](../docker-compose.yml) instead. Populated in a later phase per [docs/development-roadmap.md](../docs/development-roadmap.md).
