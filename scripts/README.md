# Scripts

Reserved for repository automation (e.g. codegen, data seeding, release tooling). Empty as of Phase 1 — current setup/lint/test/build commands are covered by the root [Makefile](../Makefile) and each app's `package.json`/`pyproject.toml` scripts. Populated as concrete automation needs arise in later phases.
