# Root environment variables consumed by docker-compose.yml for local development.
# Copy to .env and adjust as needed. Never commit the real .env file.

POSTGRES_USER=mehndiverse
POSTGRES_PASSWORD=mehndiverse
POSTGRES_DB=mehndiverse
POSTGRES_PORT=5432

REDIS_PORT=6379

API_PORT=8000
