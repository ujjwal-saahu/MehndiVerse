# Flutter does not read .env files directly. This documents the environment
# variables a later phase will wire up via --dart-define (see AppEnvironment
# in lib/core/config/app_environment.dart). Never commit real secrets here.

API_BASE_URL=http://localhost:8000
