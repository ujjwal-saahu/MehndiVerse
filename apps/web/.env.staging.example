# Staging — see docs/environments.md#staging. Real value lives in the
# GitHub Environment "staging" secrets, injected at build/deploy time.
NEXT_PUBLIC_API_BASE_URL=https://staging-api.mehndiverse.example
