# Production — see docs/environments.md#production. Real value lives in
# the GitHub Environment "production" secrets, injected at build/deploy
# time, gated by required reviewers.
NEXT_PUBLIC_API_BASE_URL=https://api.mehndiverse.example
